services:
  redis-ingestion:
    image: redis:7.2-alpine
    container_name: jtt808-redis-cache
    ports:
      - "6379:6379"
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data: